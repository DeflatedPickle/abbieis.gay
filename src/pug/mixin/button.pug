mixin button(field="", link="", type="logo")
    div&attributes(attributes)
        a(href=`https://${link}`)
            if (type != "")
                img(src=`../../res/${type}/${field}.svg`).button
            else
                div.title.button= field